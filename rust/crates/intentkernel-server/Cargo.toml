[package]
name = "intentkernel-server"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Kernel-side IPC server — bridges intentos-kernel over ikrl-transport (local + mTLS)"

[features]
tls = ["ikrl-transport/tls"]

[dependencies]
intentos-kernel = { path = "../intentos-kernel" }
intentos-audit = { path = "../intentos-audit" }
ikrl-transport = { path = "../ikrl-transport" }
ikrl-remote = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
