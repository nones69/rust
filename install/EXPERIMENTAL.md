# Experimental — install scripts

Cross-platform “upgrade layer” installers. Not the primary IntentKernel verification path.

- Documented entry: `install/linux/install.sh` (and `upgrade.sh`)
- Root `install/README.md` describes broader platform packages that are incomplete here
  (Windows/Android/ChromeOS folders are not present in this checkout).

Prefer `rust/` + `BUILD.md` for the reference prototype.
