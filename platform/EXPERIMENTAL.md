# Experimental — Python control surface

Flask/daemon UI under `platform/` for IntentOS control experiments.

- Entry: `platform/app.py` (requires `platform/requirements.txt`)
- Not required to build or test the Rust reference runtime.

Status: prototype / research. Do not treat as production host security.
