name: Main

on:
  workflow_dispatch:

permissions: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow metadata placeholder
        run: echo "Use build.yml and ip-discrambler.yml for active CI."
