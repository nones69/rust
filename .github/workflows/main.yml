name: Main

on:
  workflow_dispatch:

permissions: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow metadata placeholder
        run: echo "Use rust.yml (prototype) and build.yml / iso-build.yml for active CI."
